# パフォーマンス (v1.8.0)

v1.8.0 は動作互換を保ったままの高速化リリースです。すべての最適化は
既存のテストスイート (サーバー 201 / フロントエンド 279) と挙動パリティ
テストの下で行われています。

## 計測結果サマリ

計測環境: Linux コンテナ, Node v22.22.2, ローカルビルド。値は複数回実行の
中央値です (環境により絶対値は変動しますが、比率はおおむね再現します)。

### CLI / サーバー

| 項目                                       | v1.7.1  | v1.8.0  | 改善      |
| ------------------------------------------ | ------- | ------- | --------- |
| `yome --version`                           | 183 ms  | 60 ms   | **3.1x**  |
| `yome --status` (サーバーなし)             | 208 ms  | 116 ms  | **1.8x**  |
| 起動済みサーバーへのファイル追加           | 212 ms  | 123 ms  | **1.7x**  |
| サーバー起動 → HTTP ready (120 ファイル)   | 233 ms  | 172 ms  | **1.4x**  |
| 検索コア: 2.5MB 文書 / ヒットなし          | 22.2 ms | 1.7 ms  | **13.0x** |
| 検索コア: 2.5MB 文書 / 高頻度ヒット        | 5.3 ms  | 2.3 ms  | **2.3x**  |
| ディレクトリ走査 `walkFiles` (4,000 files) | 321 ms  | 4.7 ms  | **68x**   |
| glob 展開 (2,000 マッチ / 4,000 files)     | 12.9 ms | 8.5 ms  | **1.5x**  |
| 静的アセット配信 (同一 4MB ファイル)       | 10.8 ms | 7.4 ms  | **1.5x**  |
| `GET /` (index.html)                       | 0.80 ms | 0.60 ms | **1.3x**  |

### フロントエンド (Chromium 実測)

| 項目                          | v1.7.1     | v1.8.0     | 改善         |
| ----------------------------- | ---------- | ---------- | ------------ |
| 初期バンドル JS (minify)      | 2,713 KB   | 1,567 KB   | **-42%**     |
| 初期バンドル JS (gzip 転送)   | 641 KB     | 327 KB     | **-49%**     |
| DOMContentLoaded              | 264–309 ms | 166–186 ms | **~1.6x**    |
| 初回コンテンツ表示 (cold)     | 416–515 ms | 316–408 ms | **~1.3x**    |
| リロード時の JS 転送量 (warm) | 3,689 KB   | **0 KB**   | キャッシュ化 |

## 何を変えたか

### ビルド / CLI 起動

- **esbuild によるバンドル化**: `dist/` を per-module の tsc 出力から、
  コード分割された少数の ESM チャンクに変更。Node が起動のたびに
  100 以上のモジュールを解決・パースするコストを排除。型検査は従来どおり
  `tsc --noEmit` で実施。
- **遅延ロード**: HTTP クライアント / サーバー本体 (chokidar 含む) /
  `open` / `node:crypto` / `node:readline` / picomatch を、実際に使う
  実行パスでだけ動的 import。`yome file.md` のような日常操作は
  サーバーサブツリーを一切ロードしません。
- **`node:http` の ESM ファサード回避**: ESM `import` で `node:http` を
  読むと、Node がモジュールファサード構築時に lazy getter (fetch /
  WebSocket) を評価して undici まで読み込み、起動が約 60ms 重くなります。
  `createRequire` 経由の require に切り替えてこれを回避 (CLI とサーバー
  双方)。
- `--version` / `-V` 単独実行のフォールバックを bin エントリに実装し、
  commander のロードなしで即応答。
- 起動確認 (`waitForReady`) のポーリング間隔を短縮し、サーバー起動を
  待つ側の体感待ち時間を削減。

### サーバー

- **検索の単一パス走査**: 全文を 1 回だけ小文字化し `indexOf` でヒット位置
  へジャンプする方式に変更。行テーブルはヒットがあるときだけ、しかも
  訪問した行のぶんしか作りません。ヒットなし文書はほぼ `indexOf` 1 回分の
  コストで返ります。特殊なケースマッピング (例: `İ`) で文字数が変わる
  文書は、従来の行単位実装へ自動フォールバックし完全一致を保証
  (パリティテストを追加済み)。
- **検索対象の並列読み込み**: グループ内ファイルを小さなバッチで並列
  `readFile` (マッチ判定と結果順序は従来どおり直列・安定)。
- **ディレクトリ走査の並列化**: `walkFiles` / `walkDirs` / `expandGlob` を
  「逐次 DFS と同一の訪問順序を保ったまま」下位ディレクトリの readdir を
  並列実行する実装に変更。通常ファイルの余分な `stat()` も排除
  (シンボリックリンクのみ stat)。
- **静的アセットのメモリキャッシュ**: SPA のアセットはサーバー稼働中
  不変なので、初回読み込み後はメモリから直接応答 (stat / open /
  ストリーム生成なし)。ハッシュ付き `/assets/*` には
  `Cache-Control: public, max-age=31536000, immutable`、それ以外
  (index.html 等) には `no-cache` + ETag / 304 を付与。ブラウザの
  リロード時 JS 転送はゼロになります。ユーザーの Markdown /
  添付ファイル配信 (`raw`) は従来どおり毎回ディスクから読みます。
- ルーターにパラメータなしルートの O(1) 完全一致マップを追加。
- 検索 API がグループ全体のスナップショットではなく対象グループのみを
  コピーするように変更。

### フロントエンド

- **重量級レンダラーの遅延ロード**: mermaid (+ダイアグラム群)、shiki
  (+oniguruma WASM)、KaTeX を初期バンドルから分離し、初回使用時に動的
  import。初回描画完了後のアイドル時間にプリフェッチするため、2 個目
  以降のドキュメントでは実質ロード済みです。
- **KaTeX のゲーティング**: `$` が 2 個未満の文書は remark-math が数式を
  生成し得ないため、プラグインを完全にスキップ (出力は同一)。数式を
  含み得る文書はプラグイン到着まで Loading 表示を維持し、生 TeX の
  ちらつきを防止。
- **ハイライト結果キャッシュ**: 同一コードブロックの再ハイライト
  (ライブリロード、Raw 切り替え、タブ往復) をメモリキャッシュから同期
  適用。
- ズームモーダル (react-zoom-pan-pinch) を `React.lazy` 化。
- プリフェッチは「初回コンテンツ描画後 (またはファイルなしセッション)」
  にのみ開始し、クリティカルパスと帯域/CPU を奪い合わないよう制御。

## 計測方法

- CLI / サーバー: `spawnSync` 実測 (12–14 回の中央値)、HTTP は逐次
  リクエストの平均 (ウォームアップ後 120–400 回)。
- 検索 / 走査コア: 実装関数のマイクロベンチマーク (2.5MB 文書、
  40 ディレクトリ × 100 ファイルのツリー)。
- フロントエンド: Playwright + ローカル Chromium。新規コンテキストでの
  ナビゲーション開始から `.markdown-body h1/h2` 出現までを 9 回計測した
  中央値、および同一コンテキスト 2 回目ロードの転送量。

いずれも挙動を変えないことを最優先にしており、出力・API レスポンス・
URL・ID・イベント列は v1.7.1 と互換です。
